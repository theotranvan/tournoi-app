# DEPRECATED — do not add dependencies here.
#
# Dependencies are declared in backend/pyproject.toml, which is the single
# source of truth for both CI (`pip install ".[prod,dev]"`) and the production
# image (`pip install ".[prod]"`, see backend/Dockerfile).
#
# This file previously drifted from pyproject.toml (it was missing
# django-storages), which meant CI validated a different dependency set than
# the one running in production.
#
# To get a pinned lockfile instead:
#     pip install pip-tools
#     pip-compile --extra prod backend/pyproject.toml -o backend/requirements.lock
